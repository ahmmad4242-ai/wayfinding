# 🚀 دليل البدء السريع - Floor Plan Analyzer

## نظرة سريعة

**محلل مخططات الطوابق** - أداة ذكية لتحليل المخططات المعمارية تلقائياً مع:
- ✅ استخراج العناصر (جدران، أبواب، غرف)
- ✅ حساب المساحات والمقاييس
- ✅ تحليل التوجيه والمسارات
- ✅ فحص الامتثال للكود
- ✅ **تحليل لوني متقدم** (جديد!)
- ✅ رسوم بيانية تفاعلية

---

## النشر السريع (3 خطوات)

### 1. نقل الملفات للـ VPS

```bash
# من جهازك المحلي
scp -i ~/.ssh/your-key floor-plan-analyzer-deploy.tar.gz root@77.37.35.25:/root/

# الاتصال بالـ VPS
ssh root@77.37.35.25

# فك الضغط
mkdir -p /opt
tar -xzf /root/floor-plan-analyzer-deploy.tar.gz -C /opt/
```

### 2. إعداد البيئة

```bash
cd /opt/floor-plan-analyzer

# نسخ وتعديل .env
cp .env.example .env
nano .env

# غيّر هذه القيم المهمة:
# DB_PASSWORD=كلمة_سر_قوية
# SECRET_KEY=مفتاح_عشوائي_طويل
# ALLOWED_ORIGINS=https://flows.aqeeli.com,https://wfapi.aqeeli.com
```

### 3. النشر

```bash
# تثبيت Docker (إذا لم يكن مثبتاً)
curl -fsSL https://get.docker.com | bash

# تشغيل سكريبت النشر
chmod +x infra/deploy.sh
sudo ./infra/deploy.sh

# انتظر 2-3 دقائق للتحميل والبناء...
```

**✅ تم! الآن API يعمل على `http://77.37.35.25:8000`**

---

## الاختبار السريع

```bash
# فحص الصحة
curl http://localhost:8000/health

# رفع ملف تجريبي
curl -X POST http://localhost:8000/api/analyze \
  -F "file=@test_plan.pdf" \
  -F "building_type=hospital" \
  -F "enable_color_analysis=true"
```

---

## الوصول للواجهة

افتح المتصفح:
- **محلياً**: http://77.37.35.25:8000
- **API Docs**: http://77.37.35.25:8000/api/docs

---

## الخطوات التالية

### إعداد النطاق و SSL

```bash
# 1. تثبيت Nginx
apt-get install nginx -y

# 2. إنشاء ملف الإعدادات
nano /etc/nginx/sites-available/floor-plan-analyzer

# 3. نسخ الإعدادات من DEPLOYMENT_GUIDE_AR.md
# 4. تفعيل الموقع
ln -s /etc/nginx/sites-available/floor-plan-analyzer /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx

# 5. SSL باستخدام Certbot
apt-get install certbot python3-certbot-nginx -y
certbot --nginx -d flows.aqeeli.com -d wfapi.aqeeli.com
```

---

## الأوامر الأساسية

```bash
cd /opt/floor-plan-analyzer

# عرض Logs
docker logs -f fpa_api

# إعادة التشغيل
docker-compose restart

# الإيقاف
docker-compose down

# التشغيل
docker-compose up -d

# التحديث
./infra/update.sh
```

---

## استكشاف الأخطاء

### API لا يستجيب

```bash
# فحص الحاويات
docker ps

# عرض Logs
docker logs fpa_api

# إعادة التشغيل
docker-compose restart api
```

### خطأ في رفع الملف

```bash
# تأكد من حجم الملف < 50MB
# تأكد من صيغة الملف (PDF, PNG, JPG)

# زيادة الحد في Nginx
client_max_body_size 100M;
```

---

## المميزات الجديدة: التحليل اللوني 🎨

عند رفع ملف، فعّل خيار "التحليل اللوني" للحصول على:

- 🎨 **الألوان السائدة**: أكثر 5-10 ألوان ظهوراً مع النسب
- 📊 **إحصائيات لونية**: السطوع، التباين، درجة الحرارة
- 🗺️ **خرائط حرارية**: توزيع الكثافة اللونية
- 💡 **توصيات**: اقتراحات لتحسين قابلية القراءة

**مثال JSON:**
```json
{
  "color_analysis": {
    "dominant_colors": [
      {"hex": "#ffffff", "percentage": 45.2, "name": "أبيض"},
      {"hex": "#000000", "percentage": 25.1, "name": "أسود"}
    ],
    "statistics": {
      "brightness_avg": 185.3,
      "contrast_ratio": 3.2,
      "temperature": "cool"
    }
  }
}
```

---

## الدعم والمساعدة

📖 **التوثيق الكامل**: 
- README_AR.md - شرح شامل للميزات
- DEPLOYMENT_GUIDE_AR.md - دليل النشر المفصل

🐛 **الإبلاغ عن مشاكل**:
- GitHub Issues
- logs: `docker logs fpa_api`

💬 **الأسئلة الشائعة**:
راجع قسم "استكشاف الأخطاء" في README_AR.md

---

## بنية المشروع

```
floor-plan-analyzer/
├── src/                      # Backend (FastAPI)
│   ├── parser/              # معالجة الصور
│   ├── detection/           # كشف العناصر
│   ├── analysis/            # التحليلات
│   ├── wayfinding/          # التوجيه
│   ├── compliance/          # الامتثال
│   ├── coloranalysis/       # التحليل اللوني (جديد!)
│   └── api/                 # REST API
├── frontend/                # واجهة الويب
├── infra/                   # ملفات النشر
│   ├── deploy.sh           # سكريبت النشر
│   └── update.sh           # سكريبت التحديث
├── docker-compose.yml       # Docker setup
└── README_AR.md             # التوثيق الكامل
```

---

## التحديثات المستقبلية

- [ ] تحسين دقة كشف العناصر باستخدام نماذج أكبر
- [ ] دعم ملفات DWG/DXF مباشرة
- [ ] تصدير IFC للتكامل مع BIM
- [ ] لوحة Power BI للإدارة
- [ ] API للمحاكاة 3D

---

**تم التطوير بواسطة**: محلل مخططات الطوابق © 2024

🚀 **ابدأ الآن واستمتع بتحليل ذكي للمخططات!**
